.channel(layout='column')
  .header {{channel.name}}
  div(layout='row' flex)
    div(flex)
      plyr(source='channel.track.sources')
      .title {{channel.track.title}}
    .panels panels
  .footer
